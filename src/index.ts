import * as shuffle from './utils';

export default shuffle;

export const {
	alphabets,
	capitalize,
	generateRandomString,
	lowerCaseLetters,
	shuffleString,
	symbols,
	upperCaseLetters,
} = shuffle;
